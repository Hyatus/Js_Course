// Métodos más utilizados con los Strings 

const producto = "Monitor de 20 pulgadas";

// Cambia a mayúsculas todas las letras
console.log(producto.toUpperCase());


// Cambia a minúsculas todas las letras 

console.log(producto.toLowerCase());


const precio = 300;
console.log(precio); // Lo imprime como un número
console.log(precio.toString()); // Lo convierte en un String 

